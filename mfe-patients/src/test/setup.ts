import "@testing-library/jest-dom";
// This import extends Vitest's `expect` with jest-dom matchers
// like toBeInTheDocument(), toHaveTextContent(), toBeDisabled() etc.
// Without this, those matchers don't exist and your tests will error.
